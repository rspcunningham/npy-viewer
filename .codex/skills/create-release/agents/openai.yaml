interface:
  display_name: "Create Release"
  short_description: "Draft notes, tag, push, and release"
  default_prompt: "Use $create-release to draft release notes, create an annotated tag, and push it to trigger the release workflow."
